<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
    <!-- allow outgoing network connections -->
    <key>com.apple.security.network.client</key>
    <true/>

    <!-- allow incoming network connections (gateway) -->
    <key>com.apple.security.network.server</key>
    <true/>

    <!-- allow unsigned executable memory (python, node) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>

    <!-- allow jit compilation (node/v8 engine) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>

    <!-- disable library validation for bundled binaries -->
    <key>com.apple.security.cs.disable-library-validation</key>
    <true/>

    <!-- disable executable memory protection for pyside6/qt -->
    <key>com.apple.security.cs.disable-executable-page-protection</key>
    <true/>
</dict>
</plist>
